import { Tickets } from './tickets';

export class TicketsDtl {
    id: string;
    sender: string;
    idTickets: Tickets;
    date: Date;
    description: string;
    createdBy : String;
    updatedBy : String;
}